<h1>Listing people</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @people.each do |person| %>
  <tr>
    <td><%= person.name %></td>
    <td><%= link_to 'Show', plain_person_path(person) %></td>
    <td><%= link_to 'Edit', edit_plain_person_path(person) %></td>
    <td><%= link_to 'Destroy', plain_person_path(person), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Person', new_plain_person_path %>
