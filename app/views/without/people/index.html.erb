<h1>Listing people</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @people.each do |person| %>
  <tr>
    <td><%= person.name %></td>
    <td><%= link_to 'Show', without_person_path(person) %></td>
    <td><%= link_to 'Edit', edit_without_person_path(person) %></td>
    <td><%= link_to 'Destroy', without_person_path(person), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Back', '/' %> |
<%= link_to 'New Person', new_without_person_path %>
